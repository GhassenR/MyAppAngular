
---
#installnodemudoles
                - name: install node modules
                  shell: npm install
                - name: build app
                  shell : npm install --save-dev @angular-devkit/build-angular && ng build
